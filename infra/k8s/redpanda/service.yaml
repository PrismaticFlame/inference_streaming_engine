apiVersion: v1
kind: Service
metadata:
  labels:
    app: redpanda
  name: redpanda
spec:
  ports:
  - name: kafka
    port: 9092
    protocol: TCP
    targetPort: 9092
  selector:
    app: redpanda
  type: ClusterIP
